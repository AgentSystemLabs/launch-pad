# Engineering loops

Each file is a **single-session** task prompt for one `cursor-agent` run.
Workers rotate loops by replica index; override by editing `worker/run-loop.sh`.

Target app: [The Daily Grind](https://github.com/YOUR_ORG/coffee-shop) — multiplayer 3D coffeeshop (vanilla JS + Three.js + Vite + Node/ws).

Keep every PR **under ~150 lines changed** when possible.
