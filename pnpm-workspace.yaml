packages:
  - "packages/*"
  - "examples/*"
  - "e2e"

allowBuilds:
  esbuild: true

# esbuild ships a native binary built via a postinstall script; tsup/tsx/vitest
# all depend on it, so this build must be allowed to run.
onlyBuiltDependencies:
  - esbuild
